using Microsoft.AspNetCore.Mvc;

namespace Daylon.RateMeApp.Api.Controllers
{
    [Route("api/[controller]")]
    [ApiController]
    public class ProductController : Controller
    {




        // POST
        // bombar dps, completar

        //public async Task<IActionResult> CreteProductAsync([FromBody] RequestCreateProductJson request) //criar request json no shared commmunication
        //{
        //    if (!ModelState.IsValid)
        //        return BadRequest(ModelState);

        //    var product = await _productService.CreateProductAsync(request); // criar service e interface

        //    return Created("", product);
        //}
    }
}

//   /ᐠ - ˕ -マ D A Y L O N